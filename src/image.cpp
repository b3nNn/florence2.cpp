#include "image.hpp"

Image Image::open(const std::string& url) {
    // Logic to download the image from the URL using a streaming HTTP request
    // and open it as an image object
    return Image();
}

int Image::width() const {
    // Logic to return the width of the image in pixels
    return 0;
}

int Image::height() const {
    // Logic to return the height of the image in pixels
    return 0;
}